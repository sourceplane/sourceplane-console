# @sourceplane/sdk

Workspace scaffold for the public TypeScript SDK.

The current implementation is intentionally narrow: a typed HTTP client wrapper with shared envelope/error handling. Future tasks can add richer route helpers once the public API surface stabilizes.
