# @sourceplane/ui

Shared UI primitives for the web console.

The current components are intentionally generic and contract-friendly so later UI work can layer on richer design without coupling to internal Worker code.
