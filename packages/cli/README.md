# @sourceplane/cli

Workspace scaffold for the public Sourceplane CLI.

This package currently exposes a small placeholder command surface wired to the SDK. Later tasks can replace the minimal parser with a richer command graph without changing the public API dependency boundary.
