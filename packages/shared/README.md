# @sourceplane/shared

Generic helpers only.

This package is the safe place for:

- request IDs and response envelopes
- deployment-stage parsing
- Cloudflare binding helper types

This package must not accumulate domain logic.
