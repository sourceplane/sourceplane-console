# metering-worker

Placeholder bounded-context folder reserved for usage metering.

Implement against `specs/components/10-metering.md` and keep ingestion and rollups contract-driven so billing can layer on later.
# metering-worker

Reserved bounded-context folder for usage ingestion and rollups. Billing should consume stable usage contracts from this worker instead of reimplementing them.
