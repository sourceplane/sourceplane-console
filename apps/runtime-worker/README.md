# runtime-worker

Placeholder bounded-context folder reserved for orchestration and reconciliation.

Implement against `specs/components/08-runtime-orchestration.md` and consume component manifests rather than hardcoding per-component flows.
# runtime-worker

Reserved bounded-context folder for Cloudflare Workflows orchestration, reconciliation, and deployment state transitions. Keep orchestration driven by the shared resource and component contracts.
