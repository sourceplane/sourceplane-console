# resources-worker

Placeholder bounded-context folder reserved for resources and the component registry.

Implement against `specs/components/06-resources-and-component-registry.md` and consume shared resource/component contracts rather than inventing local schema variants.
# resources-worker

Reserved bounded-context folder for Sourceplane resources and component-backed desired state. Runtime orchestration should consume contracts from here, not bypass them.
