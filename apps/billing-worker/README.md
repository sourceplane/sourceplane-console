# billing-worker

Placeholder bounded-context folder reserved for billing and entitlements.

Implement against `specs/components/11-billing.md` after metering is in place. This component must stay behind the public API and shared billing contracts.
# billing-worker

Reserved bounded-context folder for plans, subscriptions, entitlements, and provider adapters. Keep billing behind public contracts and avoid UI-only entitlement logic.
