# membership-worker

Placeholder bounded-context folder reserved for organizations and membership.

Implement against `specs/components/04-organizations-membership.md`. This component should own organization, invite, and role-assignment facts.
# membership-worker

Reserved bounded-context folder for organizations, invites, memberships, and role assignments. Keep tenant ownership facts here rather than in shared helpers.
