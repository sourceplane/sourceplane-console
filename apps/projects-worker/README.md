# projects-worker

Placeholder bounded-context folder reserved for project and environment ownership.

Implement against `specs/components/05-projects-environments.md` without introducing cross-domain table reads.
# projects-worker

Reserved bounded-context folder for project and environment ownership. Later implementations should own their own persistence and publish contracts instead of cross-importing other domains.
