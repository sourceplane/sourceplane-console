# events-worker

Placeholder bounded-context folder reserved for events, audit, and observability.

Implement against `specs/components/09-events-audit-observability.md` and keep the event envelope stable through the shared contract package.
# events-worker

Reserved bounded-context folder for event fanout, audit queries, and observability surfaces. Mutations in other domains should emit stable event contracts consumed here.
