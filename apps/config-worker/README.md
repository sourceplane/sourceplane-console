# config-worker

Placeholder bounded-context folder reserved for config, secrets, and flags.

Implement against `specs/components/07-config-secrets-flags.md` and keep secret material behind Worker secrets or Secrets Store flows.
# config-worker

Reserved bounded-context folder for config metadata, secret references, and feature-flag surfaces. Real secret values should remain in Wrangler and Cloudflare secret management flows.
